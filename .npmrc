engine-strict=true
# Peers are curated in pnpm-workspace.yaml (peerDependencyRules); don't hard-fail installs.
strict-peer-dependencies=false
# Keep the lockfile honest and reproducible.
resolution-mode=highest
